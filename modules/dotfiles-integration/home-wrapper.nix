# This module provides shared home-manager modules for dotfiles integration
{ lib, ... }:

{
  # No longer needed - configuration is now handled at user level
  # This file is kept for backward compatibility but does nothing
}